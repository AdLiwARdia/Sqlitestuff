an assignment for networking and data structures. 
task was to separate a table into 5 or more smaller tables that have a good relationship with each other
gameworld.bat sets up all thos tables and the query.bat files asks to print out some cells from the tables, some asks the table to make changes.
the_runall.bat file runs all the bat files in a sequence